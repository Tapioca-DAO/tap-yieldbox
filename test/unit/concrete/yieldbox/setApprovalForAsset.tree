setApprovalForAsset.t.sol
├── when `operator` is address(0)
│   └── it should revert
└── when `operator` is not address(0)
    ├── when `operator` is YieldBox address
    │  └── it should revert
    └── when `operator` is not YieldBox address
        └── when `assetId` is not supported
        │   └── it should revert
        └── when `assetId` is supported
            ├── it should update `isApprovedForAsset` from `msg.sender` to `operator` and `assetId` with the new `approved` value, with id zero
            └── it should emit an `ApprovalForAsset` event